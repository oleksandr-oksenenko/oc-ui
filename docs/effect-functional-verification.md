# Effect migration functional verification

Scope: the Effect migration, including the new renderer Connection, Workspace owner and Workspace model, integrated with main commit `a232233`. On 2026-09-05, root `pnpm check` passed, root `pnpm test` passed **655 tests across 82 files**, and a fresh `pnpm test:acceptance:mac` passed **all 5 packaged Electron scenarios**. The runner verified the bundle signature and architecture, worker and PTY shutdown, and removed the successful disposable profile. The annotation acceptance flow uses the newer inline editor from main.

Controlled tests run oc-ui controllers, Effect scopes and Solid components against injected SDK/native boundaries. Packaged acceptance uses the actual Electron renderer and pinned OpenCode server, an isolated profile/database/Git fixture, and a scripted local model provider. Native Quit confirmation is mocked.

Every scenario in the final column passed in the same packaged run. This does not imply every failure interleaving was reproduced in Electron.

Note, 2026-09-22: the native Quit confirmation and its Cancel path were removed; quitting now stops the built-in server without asking. The Quit row and the mocked-dialog wording above record the 2026-09-05 run. Current quit coverage is the [shutdown tests](../apps/desktop/src/main/shutdown.test.ts) and the packaged startup scenario, which passed on this date.

Note, 2026-09-23: session deletion no longer removes worktrees; registered worktrees are kept (see [Session deletion](session-deletion-design.md)). The Delete Session/worktree row records the 2026-09-05 packaged run. Current deletion coverage is the rewritten [deletion tests](../apps/desktop/src/renderer/components/App/ConnectedApp/Sessions/SessionSidebar/DeleteSessionFlow.test.tsx) and the browser acceptance scenario "creates an isolated server worktree and keeps it after session deletion"; both passed on this date. The updated packaged scenario has not been rerun since the change.

| Changed responsibility       | Controlled coverage                                                                                                                                                                                                                                                                                                                                                   | Real Electron + server scenarios — passed                                                                                     |
| ---------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| Main server ownership        | [Local server tests](../apps/desktop/src/main/local-opencode.test.ts): shared/lazy startup, authenticated readiness, cancellation, deadlines, failed-stop retry.                                                                                                                                                                                                      | Lazy start, health/version/PID, worker reuse, crash and explicit restart.                                                     |
| Utility worker lifetime      | [Worker tests](../apps/desktop/src/main/opencode-worker.test.ts): stop during preparation, shutdown-before-release, failed boot and cleanup settlement.                                                                                                                                                                                                               | Pinned worker boot; Quit waits for worker and native PTY exit.                                                                |
| Settings                     | [Settings tests](../apps/desktop/src/main/settings.test.ts): encrypted/private persistence, validation, FIFO, queued cancellation and native settlement.                                                                                                                                                                                                              | Saved local choice; encrypted remote credentials, reload and forget.                                                          |
| Quit                         | [Shutdown tests](../apps/desktop/src/main/shutdown.test.ts): admission, repeated Quit, Settings-before-IPC cleanup and failed-stop retry.                                                                                                                                                                                                                             | Cancel preserves worker; confirmed Quit closes child processes.                                                               |
| Connection transitions       | [App tests](../apps/desktop/src/renderer/App.test.tsx): obsolete attempts, persistence failures, local/remote crash policy and ordered workspace replacement.                                                                                                                                                                                                         | Invalid URL/password, remote connect/change/forget, local reload/restart.                                                     |
| Workspace lifetime/readiness | [Owner tests](../apps/desktop/src/renderer/workspace-owner.test.ts), [runtime tests](../apps/desktop/src/renderer/opencode/runtime.test.ts), App tests: handshake failure, subscriber loss, retained SDK root and uncancellable I/O.                                                                                                                                  | Renderer reload reconstructs workspace and reuses worker.                                                                     |
| Session catalog              | [Catalog tests](../apps/desktop/src/renderer/opencode/session-catalog.test.ts): pagination, child sessions, event/snapshot races, shared refresh/failure and shutdown.                                                                                                                                                                                                | UI-created sessions appear, switch and disappear after deletion.                                                              |
| Selection, Stop and recovery | [Workspace tests](../apps/desktop/src/renderer/components/App/ConnectedApp/Sessions/createSessionWorkspace.test.ts), [recovery tests](../apps/desktop/src/renderer/components/App/ConnectedApp/Sessions/sessionRecovery.test.ts): stale selection, duplicate Stop and ordered/coalesced reconnect refresh.                                                            | Switch sessions; Stop aborts the provider stream; socket disconnect/reconnect preserves selection and permits another prompt. |
| Transcript                   | [Transcript tests](../apps/desktop/src/renderer/opencode/transcript.test.ts): complete pagination, per-session FIFO, failure/retry, idle release and shutdown settlement.                                                                                                                                                                                             | Streamed response, populated transcript after selection change and renderer reload.                                           |
| Composer/text drafts         | [Composer tests](../apps/desktop/src/renderer/components/App/ConnectedApp/Conversation/createSessionComposer.test.ts): admission, session isolation, concurrent edits, stale completion and owner teardown.                                                                                                                                                           | Per-session drafts, send/clear, provider failure and subsequent successful prompt.                                            |
| Annotations/reviews          | Composer tests and [draft tests](../apps/desktop/src/renderer/domain/review-drafts.test.ts): immutable payloads, revision-safe clearing, restoration and late acknowledgement.                                                                                                                                                                                        | Annotation create/discard/send/reload; review survives panel reopen, reaches provider and persists after renderer reload.     |
| Agent selection              | [Agent tests](../apps/desktop/src/renderer/components/App/ConnectedApp/Conversation/createSessionAgentSelection.test.ts): server choices, stale failures, duplicate switch and retained replaced reads.                                                                                                                                                               | Real agent switch and timeline entry.                                                                                         |
| Model selection              | [Model tests](../apps/desktop/src/renderer/opencode/model-selection.test.ts): catalog failure/sharing, valid choices, switch identity and interruption.                                                                                                                                                                                                               | Switch model, timeline entry and request reaches chosen provider model.                                                       |
| Session/global forms         | [Session form tests](../apps/desktop/src/renderer/components/App/ConnectedApp/Conversation/createSessionForms.test.ts), [global form tests](../apps/desktop/src/renderer/components/App/ConnectedApp/GlobalForms/createGlobalForms.test.ts): invalidation, reply/cancel, per-form errors, duplicate admission and retained reads; component tests cover reopen/focus. | Real question-tool form reply and Cancel. Global forms remain controlled-only.                                                |
| VCS changes                  | [VCS tests](../apps/desktop/src/renderer/opencode/vcs-diff.test.ts): cache identity, invalidation, refresh failure and native settlement.                                                                                                                                                                                                                             | Working/branch comparison, real Git HEAD event refresh, panel reopen.                                                         |
| Directory/Add Project        | [Browser tests](../apps/desktop/src/renderer/ui/ServerDirectoryBrowser.test.tsx): full server location, stale reads, failed navigation/retry and teardown.                                                                                                                                                                                                            | Child/parent navigation, empty folder, narrow scrolling, Add Project, Escape/reopen/focus.                                    |
| New Session/worktree         | [Creation tests](../apps/desktop/src/renderer/components/App/ConnectedApp/Sessions/SessionSidebar/NewSessionFlow.test.tsx), [worktree tests](../apps/desktop/src/renderer/opencode/create-session-worktree.test.ts): remount, partial completion, acknowledgement races, cached-commit fallback and preparation failures.                                             | Direct session; Git worktree created at verified origin commit and registered on disk.                                        |
| Delete Session/worktree      | [Deletion tests](../apps/desktop/src/renderer/components/App/ConnectedApp/Sessions/SessionSidebar/DeleteSessionFlow.test.tsx): refreshed eligibility, shared/primary worktree protection, partial failure, retry and remount.                                                                                                                                         | Cancel preserves session/worktree; confirm removes both while preserving primary checkout.                                    |

The acceptance entrypoint is [packaged-startup.e2e.ts](../apps/desktop/test/e2e/packaged-startup.e2e.ts), with separate [connection](../apps/desktop/test/e2e/connection-flows.ts), [provider](../apps/desktop/test/e2e/provider-flows.ts), [transport](../apps/desktop/test/e2e/transport-flows.ts) and [project](../apps/desktop/test/e2e/project-flows.ts) scenarios.

## Limits and final reporting

- Native timing failures, delayed uncancellable I/O, catalog races and partial worktree failures use deterministic controlled tests; they are not all reproduced in Electron.
- Global forms and multi-page transcript pagination remain controlled-only. Multi-page catalog races use controlled tests; acceptance follows the real catalog cursor. A test-owned HTTP proxy drops actual SSE sockets to verify transport reconnect in the mounted workspace.
- The provider is scripted locally. The test exercises the real OpenCode protocol and tool execution, but does not establish compatibility with a live commercial model service.
- The pinned OpenCode location watcher watches Git HEAD, not arbitrary workspace edits. Acceptance proves that an externally edited file appears in the server diff, then observes a real HEAD event and verifies renderer refresh. There is no normal-state Refresh control.
- Coverage presence is not proof that every possible execution path was tested.

## Original verification change size (before main integration)

This testing task changed no production code: **+0 / −0, net 0**. Tests and fixtures: **+876 / −4, net +872**. Documentation: **+42 / −0, net +42**. Generated code: **+0 / −0**. The additions are the connection, provider, transport and Git acceptance scenarios, a local scripted provider and a socket-dropping HTTP proxy. They exercise real boundaries without external accounts or model charges; no production abstraction or coordinator was added.
