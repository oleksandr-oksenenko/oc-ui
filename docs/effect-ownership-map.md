# OpenCode launcher investigation

Source findings recorded after rebasing to main `f9554f8`, with OpenCode `0.0.0-beta-18866` and Effect `4.0.0-rc.112`.

The ownership map has been developed into [the high-level application design](/Users/alex/.codex/worktrees/b7b2/oc-ui/docs/effect-architecture.md). That document is the design reference; this file retains the supporting investigation.

The earlier phrase “SDK compatibility boundary” means the existing SDK data store inside Workspace. It is application code retained during migration, not a separate architectural owner. Its cache, optimistic updates, rollback and event behavior must have a verified replacement before it is removed.

## What the new launcher changes

1. **The process is app-owned and lazy.** Main forks an Electron utility process on demand. It sends a fresh password and user-data path after spawn. There is no detached CLI daemon to discover or adopt. A saved local choice only preselects the form; startup still requires an explicit action.
2. **Listening is earlier than readiness.** The worker prepares the environment before importing the server, creates a scope, and invokes upstream `ServerProcess.start`. Its `onListen` callback reports the loopback address before application boot completes. Main then checks authenticated health, the exact version and this child's PID. The listener message alone is insufficient. The callback receives a shutdown signal; the shutdown effect returned by `start` only waits. The worker must close its retained scope to release resources.
3. **Main owns failed boot and termination.** Upstream can expose failed health without rejecting the worker's startup Promise. Main retains its startup deadline and health checks. Startup failure waits for child cleanup before rejecting. Stop requests allow 10 seconds for graceful exit, then two 2-second escalation waits. A missed deadline never releases the child record: only Electron's exit notification does.
4. **Quit has a retryable preparation step.** Main confirms Quit and stops the child before disposing main services. Cancel preserves the running child. If stopping fails, retain the service for another Quit attempt. Current code closes admission to new local connects once shutdown starts, even if that stop fails. A runtime finalizer alone cannot express this behavior.
5. **Configuration and storage have different roots.** The worker redirects config to `<userData>/opencode/config`, clears inherited config overrides and enables project config discovery. It explicitly chooses a persistent database because the library default is memory. The default database remains under OpenCode's data root; `OPENCODE_DB` can override it. Changing Electron user data does not isolate that database or all upstream configuration sources.
6. **Three credentials have different owners.** Main generates the temporary local HTTP password; Settings may encrypt a saved remote password; upstream OpenCode stores provider credentials in its database. None should be merged into a new desktop credential abstraction.
7. **Packaging is part of the launch boundary.** Development and packaging build the worker for Node with the pinned OpenCode library bundled, external packages staged with their dependency versions, and native/WASM assets retained. Current staging supports macOS arm64. Moving work into services must preserve this separate worker entrypoint and its runtime package assets.

Closing the last window on macOS can leave main and the worker alive; reopening creates a fresh renderer. Work already accepted by OpenCode belongs to the server and can continue without that renderer. Renderer-owned pending work cannot survive destruction of its JavaScript process; stronger guarantees would require a separate, explicitly designed main/server contract.

## Evidence and checks

The current worktree graph was checked at generation `2026-09-04T10:30:39Z`; relevant indexed source had no recorded gaps. Excluded tests, build scripts and installed package code were read directly. This was a bounded launch/connection investigation, not an exhaustive audit of every feature.

| Evidence                                                                                                                                                                                                                                                                                                                                           | What it establishes                                                                            |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| [Main process owner](/Users/alex/.codex/worktrees/b7b2/oc-ui/apps/desktop/src/main/local-opencode.ts:63)                                                                                                                                                                                                                                           | Shared startup, health, retained process ownership and escalation.                             |
| [Quit workflow](/Users/alex/.codex/worktrees/b7b2/oc-ui/apps/desktop/src/main/shutdown.ts:6), [main cleanup and IPC](/Users/alex/.codex/worktrees/b7b2/oc-ui/apps/desktop/src/main/index.ts:60)                                                                                                                                                    | Confirmation and child shutdown before disposal.                                               |
| [Worker](/Users/alex/.codex/worktrees/b7b2/oc-ui/apps/desktop/src/main/opencode-worker.ts:21), [launch settings](/Users/alex/.codex/worktrees/b7b2/oc-ui/apps/desktop/src/main/opencode-launch-settings.ts:5), [upstream server process](/Users/alex/.codex/worktrees/b7b2/oc-ui/apps/desktop/node_modules/@opencode-ai/server/dist/process.js:28) | Scope ownership, early listening, health lifecycle and persistent launch configuration.        |
| [Connection UI](/Users/alex/.codex/worktrees/b7b2/oc-ui/apps/desktop/src/renderer/App.tsx:24), [connected runtime](/Users/alex/.codex/worktrees/b7b2/oc-ui/apps/desktop/src/renderer/opencode/runtime.ts:44), [workspace composition](/Users/alex/.codex/worktrees/b7b2/oc-ui/apps/desktop/src/renderer/components/App/ConnectedApp.tsx:36)        | Current application-state owners and Solid lifetime coupling.                                  |
| [SDK data](/Users/alex/.codex/worktrees/b7b2/oc-ui/apps/desktop/node_modules/@opencode-ai/client/dist/solid/data.js:167)                                                                                                                                                                                                                           | Optimistic admission, ordered sends, acknowledgements and rollback.                            |
| [Worker build](/Users/alex/.codex/worktrees/b7b2/oc-ui/apps/desktop/electron.vite.config.ts:16), [staging](/Users/alex/.codex/worktrees/b7b2/oc-ui/tools/stage-opencode.mjs:14)                                                                                                                                                                    | Library bundling, dependency closure and native/WASM assets.                                   |
| [Packaged acceptance scenario](/Users/alex/.codex/worktrees/b7b2/oc-ui/apps/desktop/test/e2e/packaged-startup.e2e.ts:30)                                                                                                                                                                                                                           | Existing coverage for lazy launch, reload/reuse, explicit crash restart, Quit and PTY cleanup. |

During the rebase investigation, `pnpm check` passed and `pnpm test` passed: 80 files, 570 tests. Packaged Electron acceptance was inspected, not rerun. Future lifecycle implementation needs that real-process acceptance plus focused tests of pending-job cancellation and active-I/O settlement before runtime disposal; existing quit unit tests stub cleanup. The current quit error message also conflates a stop failure with a later cleanup failure, so the latter should not be described as fully recoverable.
